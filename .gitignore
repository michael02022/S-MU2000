build/
build-*/
*.o
*.a
*.exe
*.wav
roms/
# tools/make_reabank.py lee nombres del mu2000_flash.bin del usuario;
# el resultado nunca se distribuye (mismo motivo que roms/)
*.reabank

# 同じところで MAME を走らせたときに出るもの
cfg/
nvram/
snap/
__pycache__/
# MIDI files may come with incompatible licenses; keep them local
*.mid
